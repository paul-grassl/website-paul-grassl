---
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div class:list={["mx-auto max-w-5xl", noPadding ? "px-2" : "px-6"]}>
  <hr class="border-border" aria-hidden={ariaHidden} />
</div>
